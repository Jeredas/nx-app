{
  "root": "libs/db-config",
  "sourceRoot": "libs/db-config/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/db-config/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/db-config"],
      "options": {
        "jestConfig": "libs/db-config/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
